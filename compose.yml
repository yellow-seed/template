services:
  tool-sidecar:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["sleep", "infinity"]
    init: true
    volumes:
      - .:/workspace
      - /workspace/secrets
    working_dir: /workspace
