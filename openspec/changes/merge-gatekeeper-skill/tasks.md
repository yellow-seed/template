## 1. スキルファイルの作成

- [x] 1.1 `.agents/skills/merge-gatekeeper/SKILL.md` を作成する
- [x] 1.2 フロントメタ（name, description）を定義する
- [x] 1.3 環境判定セクションを追加する（ローカルPC vs Web環境）

## 2. マージ判定ロジックの定義

- [x] 2.1 CI ステータスチェック手順を定義する（`gh pr checks <pr-number>`）
- [x] 2.2 コンフリクト状態の確認手順を定義する（`gh pr view <pr-number> --json mergeStateStatus`）
- [x] 2.3 ブランチ保護ルール（ruleset）の要件確認手順を定義する

## 3. 追跡ラベル管理

- [x] 3.1 `merged-by-gatekeeper` ラベルの定義と色を決定する
- [x] 3.2 ラベルが存在しない場合の自動作成手順を定義する（`gh label create`）
- [x] 3.3 マージ前に PR へラベルを付与する手順を定義する（`gh pr edit --add-label`）
- [x] 3.4 ラベル付与済みの確認手順を定義する

## 4. マージ実行手順の定義

- [x] 4.1 マージコミット作成コマンドを定義する（`gh pr merge --merge`）
- [x] 4.2 マージタイトルは PR タイトルをそのまま使用する
- [x] 4.3 マージ後のブランチ削除確認手順を定義する
- [x] 4.4 マージ成功後のラベル状態確認手順を定義する

## 5. コンフリクトハンドリング

- [x] 5.1 コンフリクト検出時のスキップ処理を定義する
- [x] 5.2 スキップした PR の記録方法（ログ出力）を定義する
- [x] 5.3 マージ可能 PR がある場合は継続するフローを定義する

## 6. ドキュメントとテスト

- [x] 6.1 注意事項セクションを追加する（マージ取消し不可、コンフリクト発生時の対応など）
- [x] 6.2 他のスキル（pull-request, self-review, git-branch-worktree）との連携を記述する
- [x] 6.3 使用例を具体的なコマンド付きで記載する
