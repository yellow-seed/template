## なぜやるか

`shell-dev` はアプリケーションそのものではなく、検証や整形などの開発補助ツールを安定して実行する sidecar 的な役割を担っている。名前と AGENTS.md の説明をその目的に合わせることで、テンプレート利用先でもコンテナの責務を誤解しにくくする。

## Ref

- ユーザー依頼: shell-dev を sidecar 目的に沿ってリネームし、`docker compose up` / `exec` / `down` の運用へ改善
